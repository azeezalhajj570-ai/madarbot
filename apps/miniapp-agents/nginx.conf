server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location /webapp/agents/ {
    alias /usr/share/nginx/html/;
    try_files $uri $uri/ /webapp/agents/index.html;
  }

  location / {
    try_files $uri $uri/ /index.html;
  }
}
